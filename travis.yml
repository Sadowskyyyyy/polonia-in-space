language: php
php:
  - '7.4'
  - '8.0'
env:
  - DB=mysql
  - TEST_SUITE=units
  - TEST_SUITE=integration

before_script: composer require --dev phpunit/phpunit
script: php bin/phpunit tests

before_install:
  - brew install git-lfs