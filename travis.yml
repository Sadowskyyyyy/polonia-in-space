language: php
php:
  - '7.4'
  - '8.0'
env:
  -DB=mysql
  -DB=postgres
  - TEST_SUITE=units
  - TEST_SUITE=integration
script:
  - make ci
install:
  - composer install

os: osx

before_install:
  - brew install git-lfs

before_script:
  - git lfs pull